<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <style type="text/css">
        ul {
            display: flex;
        }

        ul a {
            margin-right: 5px;
        }
        </style>
    </head>
    <body>
    <ul>
    <a href="{{ path('contact_list') }}">Liste des contacts</a>
    <a href="{{ path('tag_list') }}">Liste des catégories</a>
    </ul>
    </body>
</html>